@model IEnumerable<CadastroEntidadesRelacionadas.Models.EntidadeA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._EntidadeB.Endereco)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._EntidadeB.Endereco)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EntidadeAID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EntidadeAID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EntidadeAID })
        </td>
    </tr>
}

</table>
